# KittyLang
A programming language by cats for cats

# Interpreter
jcat - implemented in java

### To run the interpreter(at it's current start) via command line use:
  - `java -jar jcat.jar <filename>.kln` to run a file
  - `java -jar jcat.jar` to open up the interactive REPL
 
